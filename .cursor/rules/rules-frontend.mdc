---
description: Полная переработка фронтенда с современным стеком и созданием сайта-резюме с интеграцией с Java backend
globs: ["src/**/*", "package.json", "vite.config.ts", "tailwind.config.js", "postcss.config.js", ".env*", "tsconfig.json"]
alwaysApply: true
---

# Цель
Создать **современный сайт-резюме** на **React 18 + TypeScript + Vite + Tailwind CSS**, удалить лишнее, построить правильную архитектуру, подключить Java backend API (для хранения и получения данных резюме).

# Архитектура
src/
  assets/      # картинки, шрифты, фото
  components/  # UI-компоненты
  pages/       # страницы (Главная, О себе, Портфолио, Контакты)
  hooks/       # кастомные хуки
  services/    # API-запросы (axios)
  store/       # глобальное состояние (например, Zustand или Redux Toolkit)
  types/       # типы данных
  utils/       # утилиты
  config/      # конфигурация API и настроек

# Основные задачи
- Удалить всё неиспользуемое.
- Создать чистую архитектуру.
- Сайт-резюме должен быть **адаптивным** и поддерживать **тёмную тему**.
- API-запросы через axios с базовым URL из `.env`.
- Данные (опыт работы, навыки, проекты) загружать с backend.
- Tailwind CSS для всех стилей.
- Красивый UI-шаблон в едином стиле.
- Поддержка кнопки **Скачать резюме (PDF)**.
- Возможность отправить контактную форму (POST-запрос на backend).

# Структура сайта
1. **Header** — логотип, меню:
   - Главная
   - О себе
   - Портфолио
   - Контакты
2. **Hero-блок** — фото, имя, должность, кнопка "Скачать резюме".
3. **О себе** — краткая биография, ключевые навыки.
4. **Портфолио** — карточки проектов с описанием и ссылками.
5. **Опыт работы** — список компаний, должностей, периодов работы.
6. **Контакты** — форма обратной связи (имя, email, сообщение).
7. **Footer** — копирайт, ссылки на соцсети.

# UI-шаблон
- **Card** — универсальный компонент для проектов и опыта работы.
- **Button** — стилизованная кнопка с поддержкой primary/secondary стиля.
- **ThemeToggle** — переключатель светлой/тёмной темы.
- Google Fonts: `Inter`, `Roboto`.
- Цветовая палитра:
  - Основной: `#3b82f6` (blue-500 Tailwind)
  - Вторичный: `#9333ea` (purple-600)
  - Фон: `#f9fafb` (gray-50)
  - Тёмный фон: `#111827` (gray-900)

# API-интеграция
- В `src/services/api.ts` создать axios-инстанс:
```ts
import axios from "axios";

export const api = axios.create({
  baseURL: import.meta.env.VITE_API_URL,
  withCredentials: true,
});
