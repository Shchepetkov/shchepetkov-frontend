# Максим Щепетков - Сайт-резюме

Современный многоязычный сайт-резюме автоматизатора тестирования на Java, построенный с использованием React 18, TypeScript, Vite и Tailwind CSS.

## 🚀 Технологии

- **React 18** - Современная библиотека для создания пользовательских интерфейсов
- **TypeScript** - Типизированный JavaScript для лучшей разработки
- **Vite** - Быстрый инструмент сборки
- **Tailwind CSS** - Utility-first CSS фреймворк
- **React Router** - Маршрутизация для SPA
- **Zustand** - Легковесное управление состоянием
- **Axios** - HTTP клиент для API запросов

## 📁 Структура проекта

```
src/
├── assets/          # Статические ресурсы (изображения, шрифты)
├── components/      # Переиспользуемые UI компоненты
│   ├── auth/       # Компоненты аутентификации
│   ├── layout/     # Компоненты макета (Header, Footer)
│   └── ui/         # Базовые UI компоненты
├── config/         # Конфигурация приложения
├── hooks/          # Кастомные React хуки
├── i18n/           # Интернационализация (переводы)
├── pages/          # Страницы приложения
├── services/       # API сервисы
├── types/          # TypeScript типы
└── utils/          # Утилиты и хелперы
```

## 🎨 Особенности

### ✨ UI/UX
- **Адаптивный дизайн** - Работает на всех устройствах
- **Темная тема** - Поддержка светлой и темной темы
- **Современный дизайн** - Красивый и профессиональный интерфейс
- **Анимации** - Плавные переходы и hover эффекты

### 🌍 Интернационализация
- **Двуязычность** - Поддержка русского и английского языков
- **Переключение языка** - Динамическое изменение языка интерфейса
- **Локализация** - Полная локализация всех текстов

### 🔧 Функциональность
- **Портфолио проектов** - Демонстрация работ и навыков
- **Контактная форма** - Возможность связаться
- **Скачивание резюме** - PDF версия резюме
- **Социальные сети** - Ссылки на профили

## 🚀 Запуск проекта

### Предварительные требования
- Node.js 18+ 
- npm или yarn

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd shchepetkov-frontend
```

2. **Установите зависимости**
```bash
npm install
```

3. **Настройте переменные окружения**
```bash
cp env.example .env
```

Отредактируйте `.env` файл:
```env
VITE_API_URL=http://localhost:8080/api
```

4. **Запустите проект в режиме разработки**
```bash
npm run dev
```

5. **Откройте браузер**
Перейдите по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🔧 Настройка

### API интеграция

Проект готов к интеграции с Java backend. Настройте URL API в файле `.env`:

```env
VITE_API_URL=http://your-backend-url/api
```

### Конфигурация

Основные настройки находятся в `src/config/index.ts`:

- **API настройки** - URL, таймауты, retry
- **Тема** - Настройки светлой/темной темы
- **Язык** - Поддерживаемые языки
- **Контакты** - Email, телефон, социальные сети
- **Мета-теги** - SEO настройки

### Добавление новых переводов

1. Откройте `src/i18n/translations.ts`
2. Добавьте новые ключи в объекты `ru` и `en`
3. Используйте в компонентах через `useTranslation` хук

```typescript
const { t } = useTranslation();
return <h1>{t('newKey')}</h1>;
```

## 📱 Страницы

### 🏠 Главная
- Hero секция с приветствием
- Ключевые навыки и технологии
- Кнопка скачивания резюме

### 👨‍💻 О себе
- Биография и принципы работы
- Опыт работы с технологиями
- Образование

### 🎯 Портфолио
- Проекты с описанием и технологиями
- Статистика достижений
- Ссылки на демо и GitHub

### 📞 Контакты
- Контактная информация
- Форма обратной связи
- Социальные сети

## 🎨 UI Компоненты

### Button
```tsx
<Button variant="primary" size="lg" loading={false}>
  Кнопка
</Button>
```

### Card
```tsx
<Card title="Заголовок" hover gradient>
  Содержимое карточки
</Card>
```

### Input
```tsx
<Input
  label="Email"
  type="email"
  value={email}
  onChange={setEmail}
  required
/>
```

## 🔌 API интеграция

### Настройка backend

Создайте Java backend с следующими эндпоинтами:

```java
@RestController
@RequestMapping("/api")
public class ResumeController {
    
    @GetMapping("/resume")
    public Resume getResume() { ... }
    
    @PostMapping("/contact")
    public ContactResponse sendContact(@RequestBody ContactForm form) { ... }
}
```

### Использование API

```typescript
import { resumeApi, authApi } from './services/api';

// Получить данные резюме
const resume = await resumeApi.getResume();

// Отправить контактную форму
const response = await resumeApi.sendContactForm({
  name: 'Имя',
  email: 'email@example.com',
  subject: 'Тема',
  message: 'Сообщение'
});
```

## 🚀 Деплой

### Vercel
1. Подключите GitHub репозиторий к Vercel
2. Настройте переменные окружения
3. Деплой произойдет автоматически

### Netlify
1. Соберите проект: `npm run build`
2. Загрузите папку `dist/` в Netlify
3. Настройте переменные окружения

### Docker
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "run", "preview"]
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 📞 Контакты

**Максим Щепетков**
- Email: maksim.shchepetkov@example.com
- LinkedIn: [linkedin.com/in/maksim-shchepetkov](https://linkedin.com/in/maksim-shchepetkov)
- GitHub: [github.com/maksim-shchepetkov](https://github.com/maksim-shchepetkov)
- Telegram: [@David_Rizer](https://t.me/David_Rizer)

---

⭐ Если проект был полезен, поставьте звезду!